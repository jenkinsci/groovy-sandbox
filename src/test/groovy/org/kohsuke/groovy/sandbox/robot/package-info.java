/**
 * This test demonstrates a typical use of the sandboxing,
 * where you have a set of objects that are exposed to a sandboxed groovy script for some computation.
 *
 * The sandboxed script can access those exposed objects, but nothing else.
 */
package org.kohsuke.groovy.sandbox.robot;
